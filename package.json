{
  "name": "real-time-rides",
  "version": "1.0.0",
  "main": "src/index.js",
  "dependencies": {
    "@supabase/supabase-js": "^2.10.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.0"  // Ensure the correct version for react-scripts
  },
  "scripts": {
    "start": "react-scripts start",  // For development server
    "build": "react-scripts build",  // For production build
    "test": "react-scripts test",    // For running tests
    "eject": "react-scripts eject"   // For ejecting from CRA (use with caution)
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
